import XCTest
@testable import ShadowImageView_master

final class ShadowImageView_masterTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ShadowImageView_master().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
